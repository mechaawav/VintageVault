// routes/products.js

import express from 'express';
const router = express.Router();

// Ruta para mostrar todos los productos
router.get('/', (req, res) => {
  // Lógica para obtener los productos de la base de datos y renderizar la vista correspondiente
});

export default router;
